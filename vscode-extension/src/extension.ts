import * as vscode from 'vscode';

export function activate(context: vscode.ExtensionContext) {
    console.log('Cow Language extension is now active!');

    // TODO: Initialize language server client
    // TODO: Register language features
}

export function deactivate() {
    // TODO: Cleanup when extension is deactivated
}